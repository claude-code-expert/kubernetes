# CLAUDE.md — 도커 & 쿠버네티스 교육 자료 제작 가이드

이 저장소는 도커·쿠버네티스 교육 과정의 교안을 만든다. 산출물은 브라우저에서 바로 열리는
단일 파일 HTML이며, 수강생은 문서를 위에서 아래로 따라 하면 실습이 끝나야 한다.

- 커리큘럼 정본: `CURRICULUM.md` (38개 모듈, 8개 파트, 고정 버전 매트릭스)
- 이론 정본: 『쿠버네티스 창시자에게 배우는 모범 사례 2판』(한빛미디어 2024) 21개 장.
  `book/kubernetes-example.pdf`는 44쪽 맛보기라 **앞부속·전체 목차·1장 본문만** 들어 있다.
  3장·20장 본문은 PDF에 없으므로 목차 골격 + `docs/kubernetes-monitoring/analysis/`의
  추출 노트 + 공식 문서로 채운다
- 실습 정본: 공식 문서(kubernetes.io, docs.docker.com, helm.sh, 각 프로젝트 문서).
  블로그·검색 결과는 근거로 쓰지 않는다

## 저장소 구조

```
kubernetes/
├── CLAUDE.md                이 문서 — 제작 규약
├── CURRICULUM.md            커리큘럼 목차·모듈 정의·버전 매트릭스 (정본)
├── book/kubernetes-example.pdf   원전 맛보기 (목차 + 1장)
├── course/                  ← 신규 제작물이 들어가는 곳
│   ├── index.html           전체 로드맵·모듈 목록·버전 매트릭스
│   ├── mNN-<슬러그>.html     모듈 교안 38편
│   ├── figures/             mNN-figN.svg (문서에 인라인 삽입)
│   └── labs/                실습 파일 (매니페스트·values·스크립트·앱 소스)
└── docs/                    ← 기존 자산. 참조·재사용하되 요청 없이 수정하지 않는다
    ├── chapter0 ~ chapter21   책 21개 장과 1:1 대응 HTML (ch0은 환경 구축)
    ├── kubenetes/             14일 로드맵 day01~14 + 00-index
    ├── k8s-observability-study/  관측 실습 10편 (kind 기반, 2026-08 검증)
    ├── kubernetes-monitoring/
    │   ├── CLAUDE.md          관측성 강의 전용 규약 — 문체·버전 규약의 원조
    │   ├── analysis/          책 3장·5장 추출 노트, 책↔docs 격차 분석 (정정 목록의 정본)
    │   ├── lectures/ slides/ labs/
    │   └── docs/
    ├── docker-basic/          2022년 Notion 도커 입문 — 명령이 낡음. 소재로만 참고
    ├── kubernetes-in-action/  『쿠버네티스 인 액션』 스터디 슬라이드 PDF (배경 지식)
    └── 이미지참고/             구조 다이어그램 PNG/SVG (재사용 가능)
```

신규 파일은 전부 `course/` 아래. 파일명은 영문 소문자와 하이픈만 쓴다.
한글 파일명은 로컬에서는 되지만 배포 시 깨진다.

파일명은 `mNN-<트랙>-<슬러그>.html` 세 토막이다. 트랙은 `ls` 만 봐도 어디까지가
도커고 어디부터가 쿠버네티스인지 갈리게 하려고 넣는다.

| 트랙 | 모듈 | 예 |
|---|---|---|
| `setup` | M01 | `m01-setup-environment.html` |
| `docker` | M02~M07 | `m04-docker-dockerfile-build.html` |
| `k8s` | M08~M38 | `m19-k8s-metrics-pipeline.html` |

## 교안 기본 구성 — 모든 모듈 HTML이 따르는 골격

순서를 바꾸지 않는다. 비어도 되는 블록은 ⑨뿐이다.

1. **헤더** — 모듈 번호, 제목, 한 줄 목표, 소요 시간, 난이도 단계(초/중/후반),
   검증일과 대상 버전
2. **사전 조건** — 앞 모듈이 남긴 산출물 중 무엇이 있어야 하는지 파일 경로로 명시.
   없으면 어느 모듈로 돌아가야 하는지 링크
3. **핵심 3줄** — 이 모듈이 끝나면 무엇을 **할 수 있는지**. 번호 항목 3개, 항목당
   두 문장을 넘기지 않는다. 개념 요약이나 다음 모듈 예고로 채우지 않는다 —
   "레이어는 캐시된다"가 아니라 "`history`로 어느 레이어가 캐시를 깨는지 짚는다"
4. **목차** — 절 번호와 앵커 링크
5. **왜 이걸 하나** — 앞 모듈의 결과가 왜 부족한지에서 출발한다. 2~3문단.
   "이번 시간에는 ~를 배웁니다" 같은 예고 문장은 쓰지 않는다
6. **이론** — 책의 해당 절 매핑을 명시(`책 8.3.2`). 실습에서 마주칠 개념만 쓴다.
   이론이 3화면을 넘으면 그 모듈은 쪼개야 한다는 신호다
7. **따라하기** — 아래 "실습 블록 규약" 참조. 단계마다 목적 → 명령 → 옵션 설명 →
   실행 결과 → 성공 판정
8. **일부러 실패시키기** — 최소 1개 필수. 실제로 겪을 실수(레이블 오타, 잘못된
   저장소 이름, 프로브 임계값, 존재하지 않는 이미지 태그)를 재현하고 에러 메시지를
   그대로 보여준 뒤 고친다. 이 블록이 없는 교안은 미완성으로 본다
9. **운영 관점** — 실습 규모와 운영 규모가 갈리는 지점. 해당 없으면 생략
10. **체크포인트** — "무엇이 보이면 성공인가"를 판정 가능한 문장으로. 항목은
    **명령 + 기대값** 한 쌍이다. `kubectl get po -l app=web` → `READY 3/3`,
    `curl -o /dev/null -w '%{http_code}'` → `200`. "설명할 수 있다" "안다"
    "확인했다"로 끝나는 항목은 판정이 불가능하므로 쓰지 않는다. 이해를 묻고 싶으면
    명령 항목 뒤에 `(이해 확인)` 표시를 달아 따로 둔다
11. **정리** — 리소스 삭제 명령. 부하 생성 루프를 띄웠다면 종료 방법을 반드시 포함
12. **다음 모듈 예고** — 이번 산출물이 다음 모듈의 입력이 되는 지점을 한 문단
13. **참고** — 공식 문서 URL과 접근 확인일. 책 참조는 장·절 번호까지

## 실습 블록 규약 — 명령 하나를 쓰는 방법

수강생이 복사해서 붙여넣고, 나온 화면이 문서와 같은지 대조할 수 있어야 한다.
따라하기의 모든 단계는 다음 5개를 갖춘다.

1. **목적** 한 문장 — "왜 이 명령을 지금 치는가"
2. **명령 블록** — 복사 가능한 형태. 프롬프트 기호(`$`)를 붙이지 않는다(복사 시 방해)
3. **옵션 설명** — 처음 나오는 플래그만. 표 또는 짧은 목록. 이미 설명한 플래그를
   매번 다시 설명하지 않는다
4. **실행 결과** — 실제 출력을 별도 블록으로. 길면 핵심 줄만 남기고 `...`로 줄이되
   **줄이 잘렸음을 표시**한다. 값이 환경마다 다른 부분(IP, 파드 접미사, 타임스탬프)은
   그대로 두고 "이 값은 환경마다 다르다"고 각주
5. **판정** — 결과에서 무엇을 봐야 하는지. `STATUS`가 `Running`인지, 숫자가 몇인지

```
목적: 파드가 스케줄되지 못한 이유를 이벤트에서 읽는다.

kubectl describe pod web-5d8f7c9b4-x2kqp | tail -n 12

  --- 출력 ---
  Events:
    Type     Reason            Age   From               Message
    ----     ------            ----  ----               -------
    Warning  FailedScheduling  31s   default-scheduler  0/3 nodes are available:
             3 Insufficient cpu. preemption: 0/3 nodes are available: ...

판정: Reason이 FailedScheduling, Message에 Insufficient cpu가 보이면
      requests 값이 노드 여유보다 크다는 뜻이다.
```

출력을 지어내지 않는다. 확인하지 못한 출력은 문서에 넣지 않고, 대신 "이런 형태의
출력이 나온다"고 서술하거나 실제로 실행해서 얻는다.

## 버전 정책

`CURRICULUM.md`의 버전 매트릭스가 정본이다. 2026-09-03 확인 기준 요약:

- Kubernetes **1.37.0** (지원 라인 1.35 / 1.36 / 1.37)
- kind **v0.33.0** — 노드 이미지는 반드시 `@sha256` 다이제스트까지 적는다
- Docker Engine **29.x**
- Helm **4.x** 기준. Helm 3는 2027-02-10 보안 지원 종료 — 차이는 M28에 별도 절
- Gateway API **v1.6.1** Standard. 실험 리소스는 `gateway.networking.x-k8s.io` 그룹
- kube-prometheus-stack 88.x / Prometheus 3.x / Grafana 13.x / Loki 3.7.x + **Alloy**
- Chaos Mesh **v2.8.2**, 부하는 **k6**

규칙:

- 버전 숫자를 적는 곳에는 **확인일**과 "실습 당일 재확인" 안내를 함께 적는다
- **Promtail은 쓰지 않는다** (2026-03-02 EOL). `loki-stack` 차트도 아니다
- **latest 태그 금지**를 교재 전체에서 일관되게 지킨다. 예제 매니페스트에도 태그를 박는다
- 낡은 정보는 이름을 박아서 정정한다. "검색하면 나오는 X는 지금 쓰면 안 된다"가
  이 과정의 반복 교육 포인트다
- 새 도구를 교재에 넣기 전에 최신 릴리스와 K8s 1.37 호환성을 공식 저장소에서 확인한다

## chapter3 · chapter20 검증·확장 계획

두 장은 반드시 검증하고 확장한다. 현재 상태:

| 대상 | 현재 | 문제 |
|---|---|---|
| `docs/chapter3/ch03-monitoring-logging.html` | 책 3.1~3.12 전 절 서술 + 코드 블록 10개 | 실행 결과가 없다. 실패 재현이 없다. 설치 절차가 요약본 |
| `docs/chapter3/ch03a-prom-grafana-manual.html` | Prometheus·Grafana UI 매뉴얼 | 화면 설명 위주. PromQL 실습 깊이 부족 |
| `docs/chapter20/ch20-chaos-load-experiment.html` | 책 20.1~20.3 전 절 서술 | **코드 블록 0개.** 순수 이론이라 따라 할 것이 없다 |

확장 방침:

- **chapter3 → M19~M22 4개 모듈로 분해**. 책 12개 절을 전부 유지하되 절마다 대응
  실습을 붙인다. 검증 포인트: cAdvisor·metrics-server·kube-state-metrics를 말로
  구분하지 말고 **각각을 직접 질의해 나온 메트릭 이름으로** 구분시킨다. ServiceMonitor
  라벨 연결은 성공 사례가 아니라 **먼저 실패시키고** 고치는 순서로 쓴다
- **chapter20 → M36~M38 3개 모듈로 분해**. 책이 싣지 않은 실험 정의를 직접 작성한다.
  20.1.3(통신) → NetworkChaos, 20.1.4(작동) → PodChaos·StressChaos, 20.1.5(퍼즈) →
  API 무작위 입력 실습으로 각 절에 실행 가능한 대응물을 만든다. 게임 데이 4단계는
  채워 넣는 실습 시트 양식으로 산출한다
- 책의 낡은 실습은 `docs/kubernetes-monitoring/analysis/03-책-docs-격차와-강의-설계.md`
  1절 정정 목록을 따른다 (Promtail, loki-stack, stable 저장소, Minikube, drone.io,
  SA 토큰 조회, Chaos Toolkit, cluster-admin 남발)
- 확장한 내용은 `course/`에 새로 쓴다. `docs/chapter3`, `docs/chapter20` 원본은
  요청 없이 수정하지 않는다

## HTML 규약

`docs/chapter0/ch00-local-setup.html`이 스타일 기준이다. 새 문서를 쓰기 전에 한 편을 연다.

- 단일 파일 완결. CSS는 `<style>`에 인라인, 그림은 인라인 `<svg>`로 임베드해
  파일 하나만 열어도 완전히 보인다
- 본문 폰트 Pretendard(CDN, 시스템 한글 폰트 폴백), 코드 IBM Plex Mono 계열
- CSS 변수 `--ink` `--sub` `--line` `--bg-soft` `--accent` `--warn`을 그대로 쓴다.
  색을 새로 만들지 않는다
- **폭과 글자 크기는 뷰포트에 비례한다.** 고정 860px을 쓰지 않는다.
  `html { font-size: clamp(15px, 0.304vw + 12.67px, 18.5px) }` 를 기준 크기로 두고
  문서 안의 모든 `font-size`는 `rem`으로 적는다(px 금지 — 함께 커지지 않는다).
  본문 폭은 `.wrap { width: min(92vw, clamp(720px, 78vw, 1180px)) }`.
  목차는 760px 미만에서 1단, 1400px 이상에서 3단. 라인 하이트 1.8
- 문서 최상단(`.wrap` 안, `<header>` 위)에 **과정 식별 바** `.coursebar`를 둔다.
  왼쪽은 과정명(index.html 링크), 오른쪽은 트랙 레일 `공통 M01 / 도커 M02–M07 /
  쿠버네티스 M08–M38` 중 현재 트랙에 `.on`. `<title>`은
  `[Part N · 파트이름] MNN 제목 — 도커 &amp; 쿠버네티스 과정` 형식,
  `.ch-no`는 `MODULE NN / 38 · PART N 파트이름`
- 클래스 관례 — 아래 이름만 쓰고 새로 만들지 않는다:
  `.coursebar`(과정 식별 바), `.prereq`(사전 조건), `.tldr`(핵심 3줄), `nav.toc`,
  `h2.sec`, `.step`(따라하기 한 단계), `.purpose`(목적 한 문장), `.out`·`.out-lbl`(실행 결과),
  `.verdict`(판정), `.fail`(일부러 실패시키기), `.note-2026`(버전·정정 경고),
  `.check`(체크포인트), `.deliver`(이 모듈이 남기는 파일), `.navlinks`(앞뒤 모듈),
  `dfn.term`(용어 첫 등장), `figure` + `figcaption`, `ul.tight`·`ol.tight`, `p.lead`
- 15편 이상이 같은 `<style>` 블록을 공유한다. 스타일을 고칠 일이 생기면
  **한 편만 고치지 말고 전 문서에 같은 블록을 넣는다**
- 그림은 `course/figures/mNN-figN.svg`에 파일로도 보관하고 문서에는 인라인 삽입
  (슬라이드 재사용을 위해 이중 보관). 다이어그램은 흰 배경, 회색 선, 모노스페이스 라벨로
  기존 도판과 이질감이 없게 그린다
- 코드 블록은 `<pre><code>`. 명령과 출력은 **다른 블록으로 분리**한다
- 모바일에서 표와 코드가 가로 스크롤되도록 `overflow-x: auto`를 유지한다

## 문체 규약

`docs/`의 기존 문체가 기준이다.

**금지:**
- 이모지, "여러분", "~에 대해 알아보겠습니다", "자, 이제", "간단히 말해서"
- "다양한", "강력한", "핵심적인 역할", "~하는 것이 좋습니다" 류의 빈 수식
- "결론적으로", "요약하자면"으로 시작하는 마무리 문단
- 3개 이상 연속된 불릿으로 설명을 대신하는 것. 설명은 문단으로, 불릿은 진짜 목록에만
- 모든 절을 같은 길이로 맞추는 것. 중요한 절은 길게, 아닌 절은 두 문장으로 끝낸다
- 장점/단점 표를 기계적으로 붙이는 것. 선택의 근거만 쓴다

**해야 할 것:**
- 이유를 먼저. "P99를 본다" 앞에 "평균 40ms 뒤에 숨은 3초짜리 요청 1건"
- 트레이드오프 명시. "percentiles-histogram을 켜면 시계열이 버킷 수만큼 는다. 공짜가 아니다"
- 실제 함정 경고. 검색하면 나오는 낡은 정보를 이름 박아 정정
- 명령 블록은 실행 결과 또는 성공 판정 기준과 함께
- 단정할 수 없는 것은 조건과 함께. "메모리 12GiB 미만이면 워커를 1개로 줄여라"
- 각 모듈 끝에 "이 모듈이 남기는 파일"을 적는다

## 실습 파일 규약 (course/labs/)

- 문서의 코드 블록이 정본이고 `labs/`는 그 추출본이다. 둘이 어긋나면 문서를 기준으로 맞춘다
- 디렉터리는 모듈이 아니라 **역할**로 나눈다:
  `infra/`(kind 설정) `apps/`(앱 소스·Dockerfile) `k8s/`(매니페스트)
  `charts/`(헬름 차트) `monitoring/`(values·대시보드 JSON) `chaos/`(실험 정의)
  `scripts/`(preflight·rebuild·부하 생성)
- `rebuild.sh`는 빈 클러스터에서 M14 시점 상태까지 한 번에 복구한다. 뒤쪽 모듈에서
  실습이 꼬였을 때의 탈출구다

### 실습 앱은 두 개다

| 앱 | 스택 | 위치 | 실습 구간 | 역할 |
|---|---|---|---|---|
| `journal-api` | Node 22 · Express 5 · TypeScript | `labs/apps/journal-api/` | M04~M18 | 빌드·배포·프로브·스케줄링의 대상. 책 1장 저널 예제의 최소 구현 |
| `order-api` | Spring Boot 4.1.0 · JDK 21 · Gradle | `docs/kubernetes-monitoring/labs/`에서 가져와 `labs/apps/order-api/` | M19~M38 | 관측·카오스의 대상. `/actuator/prometheus`, ChaosController 보유 |

`journal-api`는 이미지를 직접 빌드하는 실습(M04 멀티스테이지, M07 공급망)이 성립하도록
작고 빌드가 빠른 것을 쓴다. `order-api`는 M19부터 **추가로** 배포한다 — 기존
`docs/kubernetes-monitoring`의 대시보드 JSON·PromQL·ChaosController 자산을 그대로 쓰기
위해서다. 커스텀 메트릭 이름과 태그는 M20의 PromQL과 M21 대시보드가 의존하므로
임의로 바꾸지 않는다.

M19 문서는 두 앱이 왜 같이 있는지를 한 문단으로 밝힌다 — 실제 클러스터에는 언어가
다른 앱이 섞여 있고, 스크레이프 설정이 앱마다 다르다는 것 자체가 관측 파트의 교재다.
`order-api` 빌드에 JDK 21과 Gradle이 필요하므로 M01 설치 절과 preflight에 넣는다.

### 같은 앱을 두 벌로 정의하는 문제

M14 이후 `labs/k8s/journal/`(kustomize)과 `labs/charts/journal/`(Helm)이 같은 앱의
두 정의가 된다. **M15~M27은 `k8s/journal/`을 정본으로 고치고**, 차트는 M28에서 한 번에
따라잡는다. 그 사이 두 정의가 갈라지는 것은 의도된 상태이며, M28이 "왜 갈라졌는가"를
실습 소재로 쓴다. 모듈이 `k8s/journal/` 파일을 고치면 그 모듈의 "남기는 파일"에
어느 파일을 어떻게 고쳤는지 적는다

## 제작 순서

모듈을 번호순으로 만들지 않는다. 의존성이 있는 것부터 만든다.

1. `course/index.html` — 로드맵·버전 매트릭스. 다른 문서가 링크할 허브
2. M01 + `labs/infra`, `labs/scripts` — 모든 실습의 전제
3. Part 1 도커 6편 (M02~M07) — 외부 의존 없음, 병렬 제작 가능
4. M08~M14 — M14의 저널 앱이 M15~M18의 실습 대상이므로 여기서 앱과 차트를 확정
5. Part 4 관측 (M19~M22) — chapter3 확장. 여기서 `order-api`(Spring)를 두 번째 앱으로
   편입한다. Part 3보다 먼저 만들어도 된다.
   M30 카나리 판정과 M36 카오스 판정이 여기 산출물에 의존한다
6. Part 3 → Part 5 → Part 6 → Part 7
7. Part 8 (M36~M38) — chapter20 확장. 앞의 모든 산출물이 있어야 실습이 성립한다

모듈 하나를 끝낼 때마다 kind 클러스터를 새로 만들어 문서대로만 따라 해 본다.
문서에 없는 명령을 손으로 쳐야 진행된다면 그 문서는 미완성이다.

## 작업 시 주의

- `docs/` 아래 기존 HTML은 완성본이다. 참조·추출하되 요청 없이 수정하지 않는다
- `book/kubernetes-example.pdf`는 44쪽 맛보기다. 3장·20장 본문을 인용할 수 없으므로
  "책 3.4.2"처럼 목차 위치만 참조하고 내용은 공식 문서로 근거를 댄다.
  PDF 읽기는 `Read` 도구에 `pages` 파라미터 필수 (poppler 미설치 환경에서는
  pypdf를 venv에 설치해 텍스트 추출)
- 부하 생성 무한 루프를 쓰는 모듈은 종료 방법을 같은 화면에 적는다
- 책 그대로 가르치면 안 되는 항목은 정정 목록을 따르고, 격차 자체를 교육 소재로 쓴다
- 분량 때문에 실습을 요약하지 않는다. 요약해야 할 상황이면 모듈을 쪼갠다
