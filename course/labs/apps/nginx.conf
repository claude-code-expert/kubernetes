# 정적 파일은 이 서버가, /api 는 api 서비스가 처리한다 (M12 인그레스 경로 라우팅의 원형).
server {
  listen 80;
  location / {
    root /usr/share/nginx/html;
    index index.html;
  }
  location /api/ {
    proxy_pass http://api:8080;
    proxy_set_header Host $host;
  }
}
