# 문서 10.3~10.4절 — 서비스
# 셀렉터 app: k8s-sample-boot 이 deployment.yml 의 파드 레이블과 같아야 한다.
apiVersion: v1
kind: Service
metadata:
  name: k8s-sample-boot-service
spec:
  type: LoadBalancer
  selector:
    app: k8s-sample-boot
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
